# eeg-motor-imagery-decoding
Install with pip install -r requirements.txt (exact versions I used, tested on macOS); requirements.in lists the direct dependencies.

## Project question 
How well does motor-imagery decoding hold up across sessions and across people, how much calibration data does a new session need, and does the decoder rely on the physiology we expect (sensorimotor mu/beta activity)?